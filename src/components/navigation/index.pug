mixin navigation(activeLink)
    - var links = [
    - {title: 'Basic form', url: '/basic.html'},
    - {title: 'Insurance form', url: '/'},
    - {title: 'Alarm form', url: '/alarm.html'},
    - {title: 'Real Estate form', url: '/real-estate.html'},
    - {title: 'Alarm form', url: 'http://ya.ru'}
    - ];

    +b('div').navigation
        +e('ul').list
            each link in links
                +e('li').item
                    +e('a').link(href=link.url class=(activeLink === link.url) ? 'navigation__link_active' : '')!= link.title
